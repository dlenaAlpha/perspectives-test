import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row'
    },
    thoughtContainer: {
        width: '100%'
    },
    scrollView: {
        flex: 1
    }
});